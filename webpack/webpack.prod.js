// const webpack = require('webpack')

module.exports = {
  mode: 'production',
  devtool: 'source-map', // create react app recommends this for some reason
  // plugins: [
  //   new webpack.DefinePlugin({
  //     "process.env.name": JSON.stringify("sahbi-pro"),
  //   }),
  // ],
}
